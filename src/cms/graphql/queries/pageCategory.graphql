query PageCategory($slug: String!) {
    pageCategoryCollection(where: { slug: $slug }, limit: 1) {
      items {
        slug
        title
        subtitle
        metatags {
          ...ComponentMetatags
        }
        thumbnail {
          ...ComponentAsset
        }
        pageBuilder {
          ...ComponentPageBuilder
        }
      }
    }
}