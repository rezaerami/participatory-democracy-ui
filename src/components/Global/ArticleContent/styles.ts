import styled from 'styled-components';

export const StyledArticleContent = styled.article`
  border-radius: '0.8rem';
  border: solid 0.1rem var(--lightGray);
  padding: 1.6rem;
  font-size: 1.4rem;
  margin: 1.6rem 0;
`;
